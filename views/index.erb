<div class="row">
  <div class="large-12 columns">
    <h2>Just Inform Daily Top 10 Lists</h2>
    <p>We parse the form reports from <a href='http://www.reginfo.gov/'>RegInfo.gov</a> daily and provided updated top 10 lists</p>
    <hr />
  </div>
</div>

<div class="row">
  <div class="large-4 columns">
    <div class="panel">
      <h3>Burden Hours</h3>
      <ol>
        <% @burden.each do |form| %>
          <li><%= form[0]%> hours<br /><%= form[1].title %></li>
        <% end %>
      </ol>
    </div>
  </div>
  <div class="large-4 columns">
    <div class="panel">
      <h3>Cost</h3>
      <ol>
        <% @cost.each do |form| %>
          <li>$<%= form[0]%><br /><%= form[1].title %></li>
        <% end %>
      </ol>
    </div>
  </div>
  <div class="large-4 columns">
    <div class="panel">
      <h3>Responses</h3>
      <ol>
        <% @responses.each do |form| %>
          <li><%= form[0]%> responses<br /><%= form[1].title %></li>
        <% end %>
      </ol>
    </div>
  </div>
</div>